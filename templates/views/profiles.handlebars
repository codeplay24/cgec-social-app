<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>{{username}}</title>
    <style media="screen">
    body{
      background-size:cover;
      font-family: Arial;
    }
    .headerClassUserName{
      position: absolute;
      left: 1190px;
    }
    .headerClass{
      padding-bottom: 100px;
      box-shadow: 2px 4px 5px 2px rgba(0,0,0,.3);
      margin-bottom: 15px;
    }
    .headerClassNewsFeedLink{
      position: absolute;
      left: 110px;
      top: 30px;
    }

    A {
      text-decoration: none;
      color: #4CAF50;
    }

    #usernameATag{
      color: black;
    }

    .identity{
      margin: 15px auto 0;
      border 2px;
      border-color:  green;
      width: 363px;
      position: absolute;
      box-shadow: 2px 4px 5px 2px rgba(0,0,0,.3);
      border-style: solid;
      border-radius: 20px;
    }
    .innerIdentity{
      padding: 50px 50px 20px 20px;
    }
    .aPost{
      padding: 5px;
      border-bottom: 1px solid black;
    }
    .postsDone{
      margin: 15px auto 0;
      position: absolute;
      width: 700px;
      left: 410px;
      border-style: solid;
      border-radius: 20px;
      border 2px;
      border-color:  white;
      background-color: rgb(197, 240, 197);
    }
    .innerPostDone{
      padding: 50px 50px 50px 20px;
    }

    .aLink{
      color: rgb(40, 147, 173);
    }
    /* footer styles */
    .footer{
      position: absolute;
      padding-bottom: 10px;
      margin-top: 30px;
      border-top: 2px solid yellow;
      top: 570px;
      width: 1270px;
    }
    .statement{
      position: absolute;
      left:200px;
      margin-top: 20px;
    }
    .links{
      position: absolute;
      left:550px;
      down:-10px;
    }
    #githublink{
      color: black;
    }
    #linkedinlink{
      color: rgb(57, 132, 179);
    }
    #emoji{
      color: red;
    }

    </style>
  </head>
  <body>
    {{>header}}
    <div class="identity">
      <div class="innerIdentity">
        <p>@{{user.user_name}}</p>
        <h3>{{user.first_name}} {{user.last_name}}</h3>
      </div>
      <div class="socialLinks">
        <div class="innerSocialLinks">

        </div>

      </div>
    </div>
    <div class="postsDone">
      <div class="innerPostDone">
        <h4>Your posts</h4>
        {{#each posts}}
          <div class="aPost">
            <a href="/openPost?_id={{this._id}}" class="aLink"><p>{{this.user_name}} from {{this.depertment}} request a book for {{this.semester}} on {{this.subject}}</p></a>
          </div>
        {{/each}}
      </div>
    </div>
    {{>footers}}
  </body>
</html>
